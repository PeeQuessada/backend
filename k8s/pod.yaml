apiVersion: v1
kind: Pod
metadata:
  name: my-backend
spec:
  containers:
  - name: my-backend
    image: pedroquessada/my-backend:latest
    workingDir: /usr/api
    resources:
      requests:
        memory: "400Mi"
        cpu: 1
      limits:
        memory: "400Mi"
        cpu: 1
    ports:
    - containerPort: 3000